# Medieval Kingdom

A C++ game project built with Visual Studio.

## Contents
- Game source files (`*.cpp`, `*.h`)
- Visual Studio project files (`*.vcxproj`, `*.vcxproj.filters`)
- A `.gitignore` for build artifacts and IDE files

## Build
Open the solution file in Visual Studio and build the project.

## Notes
This repository contains the game source and project configuration only.